# Linked-Lists
    Functions:
*pop();

*shift();

*removeAt(index);

*insertAt(data, index);

*toString();

*find(value);

*contains(value);

*at(index);

*tails();

*heads();

*sizes();

*insertLast(data);

*insertFirst(data);